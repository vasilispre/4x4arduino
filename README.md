# 4x4arduino
A four-wheel Arduino Uno WiFi vehicle that transports an object on a rotating platform. Using a timer, it moves to a specific location, stops, and activates two servo motors to rotate the platform, dropping the object into a bucket automatically.
